# MeetCue: 응답 수집을 넘어, 회의 결정을 설명하는 조율 도구

MeetCue는 여러 사람의 가능한 시간을 모으는 데서 멈추지 않고, 주최자가 “지금 이 회의를 정해도 되는가?”를 판단하도록 돕는 반응형 프로토타입이다. 문제 정의, 의사결정 모델, UX 구조와 카피, 인터랙션, React·TypeScript 구현과 도메인 테스트를 개인 프로젝트로 진행했다.

- 현재 범위: 백엔드 없는 P0 프로토타입
- 핵심 사용자: 회의 주최자
- 구현 환경: React 19, Vite, TypeScript, `react-aria-components`, `@internationalized/date`, `lucide-react`, `sonner`, Tailwind CSS
- 현재 후보 상태: `ready` / `pending` / `impossible`

## Problem

일정 조율의 어려움은 “가능한 시간이 몇 개인가”를 세는 데서 끝나지 않는다. 응답이 일부만 모인 순간에도 주최자는 다음을 알아야 한다.

- 지금 확정할 수 있는 시작 시각이 있는가?
- 이 후보가 가능한 이유 또는 불가능한 이유는 무엇인가?
- 반드시 응답해야 하는 필수 참석자는 누구인가?
- 선택 참석자 중 몇 명의 가능 응답이 더 필요한가?
- 누구에게 다시 응답을 요청하고, 응답이 오면 결론은 어떻게 달라지는가?

기존 후보 투표형 사고를 그대로 확장하면 주최자가 시작 시각을 먼저 잘게 나열하고, 참석자는 비슷한 후보에 반복해서 답해야 한다. 결과 화면에서도 사람별 표를 다시 계산하는 일이 주최자에게 남는다. MeetCue의 문제 정의는 투표 수를 늘리는 것이 아니라, 부분 응답 상태에서도 회의 성립 조건과 다음 행동을 설명하는 것으로 전환했다.

이 문제 정의는 정량 사용자 조사 결과라고 주장하지 않는다. 제품 문서에 기록된 가설과 설계 판단을 구현 가능한 흐름으로 만든 것이다. [현재 PRD](docs/product/prd-v2.2.1.md)와 [스프린트 목표](docs/product/sprint-decision-coherence-v2.2.1.md)에 문제와 P0 범위가 정리되어 있다.

## Hypothesis

주최자에게 원본 응답표를 먼저 보여주는 대신 후보를 `ready`, `pending`, `impossible`로 판정하고, 상태를 만든 사람과 남은 가능 응답 조건을 함께 보여주면 주최자는 수작업 계산 없이 안전한 다음 행동을 선택할 수 있을 것이다.

검증할 이해 질문은 다섯 가지로 고정했다.

1. 지금 확정할 수 있는가?
2. 왜 가능한가 또는 불가능한가?
3. 반드시 응답해야 하는 특정인은 누구인가?
4. 대체 가능한 참석자 중 몇 명의 가능 응답이 더 필요한가?
5. 지금 눌러야 할 행동은 무엇인가?

PRD가 정한 사용자 검증 기준은 업무 회의를 잡아본 5명 중 4명 이상이 다섯 질문에 정확히 답하는 것이다. 이 문서 작성 시점에는 해당 사용자 검증을 수행하지 않았으므로 통과했다고 말하지 않는다.

## Model

### 입력에서 후보로 가는 흐름

주최자가 회의 가능 기간과 확보할 시간, 참석자 기준을 정한다. 주최자의 조율 가능 시간대와 참석자의 30분 단위 가용시간을 원본으로 보관한 뒤, 제품이 확보 시간 길이에 맞는 후보 시작 시각을 파생한다. 후보는 주최자가 직접 나열하는 값이 아니다.

대표 fixture는 6명의 동료와 1시간 회의지만 제품 제약은 아니다. 후보 시작 간격은 30분이고, 모든 날짜 계산과 표시는 `Asia/Seoul`을 기준으로 한다.

### 회의 기준

회의 기준을 판정 전에 정규화해 주최자가 필수 참석자와 최소 참석 인원에 중복 포함되지 않도록 했다.

| 개념 | 의미                                    |
| ---- | --------------------------------------- |
| `R`  | 필수 참석자 ID 집합. 주최자는 항상 포함 |
| `Q`  | 회의가 성립하는 최소 참석 인원          |
| `T`  | 전체 참석자 ID 집합                     |
| `H`  | 주최자 ID                               |

전원 참석 모드에서는 `R = T`, `Q = |T|`가 된다. 일부 불참 가능 모드에서는 `Q`를 `|R|` 이상 `|T|` 이하로 제한한다.

### 후보 상태

참석자별 후보 상태는 `available`, `adjustment_commit`, `unavailable`, `unknown` 중 하나로 집계한다. 응답 제출 전의 `not_started` 또는 `draft`는 모든 후보에서 `unknown`이다. 후보 전체는 다음 순서로 판정한다.

| 후보 상태    | 판정                                                                | 주최자에게 남기는 정보                               |
| ------------ | ------------------------------------------------------------------- | ---------------------------------------------------- |
| `impossible` | 필수 참석자가 어렵거나, 미응답자가 모두 가능해도 `Q`를 채울 수 없음 | 가장 중요한 차단 이유                                |
| `ready`      | 모든 필수 참석자가 가능하고, 확약 인원이 `Q` 이상                   | 지금 확정 가능한 이유와 조정 부담                    |
| `pending`    | 아직 성립 가능하지만 긍정 응답 조건이 남음                          | 필수 미응답자, 대체 가능 대상군, 필요한 가능 응답 수 |

`pending`을 단순한 “기다리는 중”으로 축약하지 않은 것이 핵심이다. 필수 미응답자는 `requiredPending`으로 이름을 표시하고, 선택 참석자 미응답자는 `optionalPendingPool`로 묶는다. 필요한 가능 응답 수는 다음처럼 계산한다.

```text
max(0, Q - (확약 인원 + 필수 미응답 인원))
```

따라서 제품은 선택 참석자 중 특정 한 사람을 자동 추천하지 않는다. 주최자가 후보와 요청 대상을 직접 선택한다. 일정 조정 확약은 `ready`를 막지 않지만 후보의 부담 정보와 정렬 기준으로 남긴다. 자세한 계약은 [현재 결정 모델](docs/product/decision-model-v2.2.md)에 있다.

## Key Decisions

### 1. 정확한 후보 입력에서 가용시간 수집으로 이동

주최자에게 09:00, 09:30처럼 후보를 미리 나열하게 하는 대신, 기본 탐색 범위를 제안하고 주최자가 제외·추가 예외만 편집하도록 했다. 확인된 조율 가능 시간대와 선택한 확보 시간으로 제품이 30분 간격 후보를 만든다. 이 구조는 입력 노동과 후보 계산을 분리한다. [가용시간 모델](docs/product/availability-window-model-decision-v2.3.md)과 [주최자 범위 결정](docs/product/host-search-scope-input-decision-v2.4.md)에 근거가 있다.

### 2. 참석자 입력은 캘린더 초안 + 3상태 예외 편집

참석자 응답 입력은 구현되어 있다. 더미 캘린더의 free/busy 상태를 타임테이블 초안으로 채운 뒤, 참석자가 `가능해요`, `일정 조정하면 가능해요`, `참석하기 어려워요` 브러시로 예외를 수정하고 저장한다. 자동으로 채워진 상태는 제출이 아니며, 저장 CTA를 누르기 전에는 주최자 판정에 반영하지 않는다. 일정 제목은 참석자 본인 화면에만 보이고 판정에는 free/busy 의미만 전달한다. [참석자 입력 결정](docs/product/participant-availability-input-decision-v2.6.md)에 이 경계와 반응형 타임테이블 계약을 기록했다.

### 3. 판정 원본과 제출 경계 분리

시간 칸의 `unset`, 제출 상태의 `draft`, 마지막 `submitted` 응답을 구분했다. 첫 제출 전 초안은 원본 UI에 값이 보여도 주최자에게는 미응답이다. 제출 확인 때만 남은 `unset`을 어려움으로 확정한다. 이미 제출한 사람이 수정 중이면 새 초안을 저장할 때까지 마지막 제출본을 판정에 유지한다. 이 규칙으로 “입력은 했지만 아직 공유하지 않은 상태”와 실제 의사결정 데이터를 섞지 않았다.

### 4. 결과 보드를 상태별로 통합

`ready`, `pending`, `impossible`을 서로 다른 화면으로 보내지 않고 같은 결과 보드에서 비교한다. 후보 카드에는 시각, 상태, 핵심 이유, 영향 인원, 다음 행동을 두고, 필요할 때 사람별 근거를 펼친다. 인접하면서 동일한 decision signature를 가진 후보만 묶고 실제 시작 시각은 보존한다. 후보 정렬에는 숨겨진 종합 점수 대신 상태와 조정 부담, 기피 표시, 미응답 조건을 사용하는 사전식 순서를 적용했다.

### 5. P0는 결정이 완결되는 데 필요한 만큼만

P0 여정은 `회의 기준 설정 → 참석자 응답 → 주최자 판단 → 한 사람에게 재요청 → 응답 변경 후 재판정 → 확정`으로 잠갔다. 실제 계정, 알림·이메일 발송, 서버 저장, 외부 캘린더 API, 장소와 화상회의 링크는 구현하지 않았다. 계정 화면과 재요청은 DEV fixture와 화면 전환으로 시연하되, 제품 기능이 실제 네트워크 발송을 완료했다고 표현하지 않는다. [P0 범위 잠금](docs/product/p0-submission-scope-lock-v2.10.md)에 보류 범위를 명시했다.

## Interaction

1. **생성** — 주최자가 회의 제목과 목적, 참석자 역할, 최소 참석 인원, 날짜 범위와 확보 시간을 입력한다. 제품은 기본 탐색 범위를 제안하고 주최자는 필요한 시간만 제외하거나 추가한다.
2. **참석자 응답** — 참여자는 자신의 fixture 링크로 진입해 회의 맥락과 응답 마감, 캘린더 초안이 반영된 타임테이블을 본다. 날짜별로 상태 브러시를 선택해 예외를 수정하고 저장한다. 다른 참석자의 응답과 후보 판정은 노출하지 않는다.
3. **주최자 판단** — 제품은 원본 표보다 먼저 현재 결론과 후보 상태를 보여준다. `pending` 후보에서는 필수 미응답자와 대체 가능한 대상군, 필요한 가능 응답 수를 분리한다.
4. **재요청과 재판정** — 주최자가 후보와 요청 대상을 직접 선택하면 시연 상태에 요청 기록을 남긴다. 선택한 후보와 회의 맥락을 유지한 채 해당 참여자 화면으로 전환하고, 응답 제출 뒤 같은 후보를 다시 계산한다. 수진의 가능 응답이 들어오면 대표 `pending` 후보가 `ready`로 바뀌는 흐름을 검증한다.
5. **확정** — `ready` 후보를 주최자가 선택해 회의 제목, 날짜, 시작·종료 시각을 확인하고 확정한다. 조정 확약자가 있는 경우 그 부담도 안내한다.

구조는 한 화면에 한 과업을 두는 Focused Task, 참석자 전용 Participant Task, 상태와 다음 행동을 앞세운 Host Decision으로 나눴다. PC는 결과 비교와 주간 타임테이블에 공간을 쓰고, 모바일은 날짜 스트립·하루 타임테이블·하단 우선 CTA로 재구성한다. 화면 구조는 [UX 아키텍처](docs/design/architecture.md), 색상·타이포그래피·상태 표현은 [비주얼 시스템](docs/design/visual-system.md)을 기준으로 했다.

## Validation / Evidence

### 코드와 자동화 테스트로 확인한 것

도메인 계산은 [결정 모델 코드](src/domain/decision-v2.ts), [가용시간·후보 파생 코드](src/domain/availability.ts), [후보 평가와 설명 코드](src/domain/evaluation.ts)에 분리했다. 현재 테스트가 보증하는 범위는 다음과 같다.

- 필수 참석자와 최소 참석 인원 정규화, 주최자 중복 방지
- `available`·`adjustment_commit`·`unavailable`·`unknown`의 보수적 집계
- 필수 참석자 불가 시 `impossible`, 선택 참석자 미응답이 남아도 조건 충족 시 `ready`
- 필수 미응답자와 대체 가능한 선택 참석자 풀의 분리
- 미응답자가 모두 가능해도 최소 인원을 채울 수 없는 경우의 `impossible`
- 마감 경과를 상태 변경이 아닌 별도 플래그로 처리
- 동일한 decision signature를 가진 인접 후보만 그룹화하고, 같은 입력이 같은 결과를 만드는지 확인
- 미제출 초안이 후보 근거에 노출되지 않음
- 주최자가 선택한 대상에게만 재요청 문구를 생성
- 수진 응답 전후 같은 후보가 `pending`에서 `ready`로 재판정됨
- 더미 캘린더 초안, 30분 후보 파생, 날짜 경계, 시간대 제거·교체·선호 표시

검증 명령은 `npm run check`, `npm run test:domain`, `npm run lint`, `npm run build`로 구성되어 있다. 이 자동화 결과는 도메인·구조의 일관성에 대한 증거이지, 실제 사용자가 제품을 이해했다는 증거는 아니다.

### 아직 확인하지 않은 것

실제 사용자 검증, 사용성 수치, 반복 사용성, 시장성, 실제 발송 성공률은 이 프로토타입에서 측정하지 않았다. 배포 이력이나 사용자 만족도도 이 사례의 성과로 주장하지 않는다. 다음 단계는 코드가 아니라 사용자가 결과 카드를 보고 위 다섯 질문에 답할 수 있는지 확인하는 것이다.

## What Changed

제품의 중심 객체와 책임이 다음처럼 바뀌었다.

| 이전 방향                                             | 현재 방향                                                                      |
| ----------------------------------------------------- | ------------------------------------------------------------------------------ |
| 주최자가 정확한 후보 시작 시각을 먼저 만든다          | 주최자가 조율 가능 시간대를 확인하고 제품이 후보를 파생한다                    |
| 후보별 응답 수를 비교한다                             | 필수 조건과 최소 인원을 기준으로 `ready` / `pending` / `impossible`을 판정한다 |
| 미응답자를 한 덩어리의 대기로 다룬다                  | 필수 특정인과 대체 가능한 대상군, 필요한 가능 응답 수를 분리한다               |
| 참석자가 반복 버튼으로 모든 칸을 직접 입력한다        | 더미 캘린더 초안에서 예외만 3상태로 수정하고 제출한다                          |
| 초안과 제출 응답의 의미가 섞일 위험이 있다            | 제출 경계를 분리하고 제출 전 값은 판정에서 `unknown`으로 유지한다              |
| 결과를 확인한 뒤 다시 계산할 책임이 주최자에게 남는다 | 응답 변경 후 같은 후보와 결과 보드가 자동으로 재판정된다                       |

이전 후보·응답 모델과 제품 초안은 [archive의 결정 모델](docs/archive/decision-model-v2.1.md), [archive의 PRD](docs/archive/prd-v2.2.md)에서 확인할 수 있다. 현재 기준 문서와 과거 문서를 섞지 않도록, 현재 계약은 [P0 범위](docs/product/p0-submission-scope-lock-v2.10.md)와 [현재 PRD](docs/product/prd-v2.2.1.md)를 우선한다.

## Next Validation

다음 검증에서는 별도 설명 없이 대표 흐름을 수행하게 한다.

1. 주최자가 6명 fixture의 회의 기준과 시간을 설정한다.
2. 참여자가 캘린더 초안을 검토하고 3상태 예외를 수정해 제출한다.
3. 주최자가 통합 결과 보드에서 한 `pending` 후보를 고른다.
4. 주최자가 필수 특정인과 대체 가능한 대상군을 구분해 요청 대상을 선택한다.
5. 한 참여자의 응답 제출 뒤 같은 후보가 왜 `ready`가 되었는지 설명하게 한다.
6. PC와 모바일에서 초안/제출 경계, 상태 브러시, 우선 CTA를 확인한다.

관찰할 항목은 첫 결정을 내리는 경로, 다섯 질문에 대한 설명 정확도, 요청 대상 선택의 이해, 초안이 아직 판정에 반영되지 않는다는 이해, 모바일에서의 시간 입력과 저장 성공 여부다. PRD의 5명 중 4명 기준을 통과하지 못하면 카피나 정보 위계부터 조정하고, 상태 의미나 역할 경계를 바꿀 때는 [제품 결정 문서](docs/product/sprint-decision-coherence-v2.2.1.md)를 다시 열어 원인을 확인한다.
