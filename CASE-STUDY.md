# Meeting Cue — Case Study

이 문서는 Meeting Cue를 포트폴리오 관점에서 설명하는 설계·구현 기록입니다. `검증된 구현`은 현재 저장소의 코드와 문서에서 확인할 수 있는 사실이고, `가설`과 `다음 검증`은 아직 사용자 검증으로 확정하지 않은 내용입니다. 사용자 조사, 성과 지표, 출시 이력은 주장하지 않습니다.

## Problem

기존 일정 투표는 후보별 표를 모으는 데는 적합하지만, 업무 회의의 확정 판단까지 설명하지 못합니다. 주최자는 다음 질문을 동시에 판단해야 합니다.

- 꼭 참석해야 하는 사람이 가능한가?
- 최소 몇 명이 모이면 회의를 진행할 수 있는가?
- `일정 조정하면 가능해요`가 몇 명이나 되는가?
- 아직 답하지 않은 사람이 있어 결정을 미뤄야 하는가?

가장 많은 표를 받은 시간이 곧 가장 안전한 시간은 아닙니다. Meeting Cue의 문제 정의는 “어떤 시간이 인기 있는가?”가 아니라 “현재 정보로 회의를 확정해도 되는가?”입니다.

## Hypothesis

다음은 설계 가설입니다. 현재 코드가 이 가설을 입증했다는 뜻은 아닙니다.

1. 주최자는 정확한 시작 시각 여러 개보다 자신이 비울 수 있는 연속 시간대를 더 자연스럽게 입력할 수 있다.
2. 참석자는 비슷한 후보에 반복 응답하는 것보다 자신의 가용시간을 한 번 표시하는 일을 더 가볍게 느낄 수 있다.
3. 필수 참석자, 최소 참석 인원, 조정 리스크, 미응답을 분리하면 주최자가 확정 근거를 더 빠르게 이해할 수 있다.
4. 기존 링크와 응답을 보존하고 필요한 시간만 다시 물으면 조율 실패 뒤의 재작업 부담을 줄일 수 있다.

## Model

### 제품 모델

제품의 기본 흐름은 다음과 같습니다.

```text
회의 맥락·참석자 역할·성립 기준
→ 주최자와 참석자의 시간 정보
→ 확보 시간과 연속 가용성으로 후보 계산
→ 필수 응답·최소 인원·조정 리스크 평가
→ 주최자의 확정 또는 추가 확인
```

주최자는 항상 참석자이며 자신이 선택한 조율 가능 시간대에는 암묵적으로 가능합니다. 참석자는 `required` 또는 `optional` 역할을 가지며, 후보 응답은 `available`, `adjustable`, `unavailable` 세 상태로 표현됩니다. 후보 평가 결과는 `confirmable`, `needs_adjustment`, `waiting_required`, `excluded`로 나뉩니다.

### availability-window v2.3 결정

v2.3의 설계 결정은 생성 단계의 기본 객체를 정확한 `후보 블록`에서 `조율 가능 시간대`로 바꾸는 것입니다.

```text
주최자의 조율 가능 시간대
+ 참석자의 가능 시간대
+ 일정 확보 시간
→ 제품이 계산한 정확한 후보
→ 기존 회의 성립 기준으로 평가
```

이 결정은 캘린더 free/busy 자동 수집이나 AI 추천을 뜻하지 않습니다. P0에서는 사용자가 30분 단위로 직접 표시한 시간대와 명시적인 판정 규칙을 사용합니다. 정확한 후보는 입력값이 아니라 응답 이후 계산되는 파생 모델입니다.

### Derived candidate model

시간대가 `09:00~12:00`, 확보 시간이 90분이면 30분 간격으로 다음 후보를 파생할 수 있습니다.

```text
09:00~10:30
09:30~11:00
10:00~11:30
10:30~12:00
```

현재 구현의 `deriveCandidatesFromAvailabilityWindows`는 `available` 상태인 시간대를 순회하고, 확보 시간이 끝나는 시각까지 30분 단위 시작점을 생성한 뒤 시작 시각 기준으로 중복을 제거하고 정렬합니다. 따라서 파생 후보끼리 겹치는 것은 입력 오류가 아니라 계산 결과의 정상적인 특성입니다. `mergeAvailabilityWindows`는 같은 소유자·상태·현지 날짜의 맞닿거나 겹치는 시간대를 합칩니다.

다만 v2.3의 전체 모델이 구현 완료된 것은 아닙니다. 현재 코드는 주최자 시간대 선택과 파생 후보 계산을 제공하지만, 참석자 화면은 아직 파생 후보마다 응답하는 방식입니다. 참석자가 30분 칸별 가용시간을 표시하고 그 결과를 후보 평가에 연결하는 부분은 계획 상태입니다.

## Key Decisions

### 1. 투표 결과가 아니라 결정 지원

제품은 표 수만 정렬하지 않습니다. `evaluateCandidate`가 필수 참석자, 최소 참석 인원, 미응답, 조정 가능 인원, 선호 조건을 읽어 후보별 상태와 이유를 만듭니다. 최종 확정은 주최자가 합니다.

### 2. 회의 종료와 일정 확보 시간 분리

주최자가 입력하는 시간은 실제 회의가 정확히 끝나는 시각이 아니라 참석자가 일정에서 비워둘 시간입니다. 현재 내부 필드명은 `durationMinutes`이며, 제품 문서에서는 이 의미를 `reservedDurationMinutes`로 명확히 하도록 결정했습니다.

### 3. 30분 시간 최소 단위

P0의 후보 시작 시각, 시간대 그리드, 확보 시간 증감은 30분 단위입니다. 현재 UI의 빠른 선택은 30·60·90·120분이고 직접 입력도 30~240분 범위의 30분 배수로 제한합니다.

### 4. 역할과 성립 기준 분리

필수 참석자와 선택 참석자를 구분하고, `모두 참석` 또는 `최소 참석 인원`을 별도로 선택합니다. 주최자는 전체 참석 인원에는 포함되지만 응답을 기다리는 대상으로 세지 않습니다.

### 5. 회복은 재시작이 아니라 이어서 조율

확정할 후보가 없을 때도 링크와 기존 응답을 유지하고, 필요한 사람에게 다시 요청하거나 새 시간만 추가합니다. 자동 발송이나 자동 확정은 P0 범위가 아닙니다.

## Interaction

아래는 현재 프로토타입에서 코드로 확인되는 상호작용입니다.

1. **생성**: 회의 이름·목적·요청자 맥락을 입력하고, 참석자를 선택한 뒤 역할과 회의 성립 기준을 정합니다.
2. **시간 조건**: 회의 가능 날짜 범위와 30분 단위의 확보 시간을 정합니다.
3. **주최자 시간대**: `AvailabilityWindowPicker`에서 PC 주간 그리드 또는 모바일 시간 목록으로 시작과 끝을 선택합니다. 선택 시간대는 수정·삭제·실행 취소할 수 있습니다.
4. **후보 계산과 공유**: 선택한 시간대에서 후보를 계산하고 응답 마감과 공유 링크를 확인합니다.
5. **참석자 응답**: 현재 구현에서는 후보별로 `가능해요`, `내 일정 조정`, `어려워요` 중 하나를 선택하고 선호 조건을 추가할 수 있습니다. 마감 전 같은 링크에서 수정할 수 있습니다.
6. **주최자 판단**: 응답 대기, 결정, 확인 필요, 회복 화면을 상태에 따라 보여주고 후보 근거와 참석자 응답을 확인한 뒤 확정합니다.
7. **확정·회복**: 확정 문구 또는 다시 요청할 문구를 복사합니다. 새 후보가 추가되면 기존 응답은 유지하고 새 후보만 다시 묻습니다.

참석자 가용시간 그리드와 그에 따른 후보별 응답 파생은 v2.3의 목표 상호작용이지만 현재 프로토타입의 미구현 영역입니다.

## Validation status / evidence

| 구분                          | 상태                     | 확인 근거                                                                                                                                                 |
| ----------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 도메인 계약                   | 구현 확인                | `src/domain/meeting.ts`의 회의·참석자·가용시간·후보·응답 타입                                                                                             |
| 시간대 병합·후보 파생         | 구현 확인                | `src/domain/availability.ts`의 `mergeAvailabilityWindows`, `deriveCandidatesFromAvailabilityWindows`                                                      |
| 후보 판단                     | 구현 확인                | `src/domain/evaluation.ts`의 `evaluateCandidate`, `evaluateCandidates`                                                                                    |
| 생성·공유·응답·결정·회복 흐름 | 구현 확인                | `src/App.tsx`의 라우트와 화면 컴포넌트                                                                                                                    |
| 주최자 시간대 선택            | 부분 구현 확인           | 데스크톱 주간 그리드와 모바일 시간 목록이 존재함                                                                                                          |
| 참석자 가용시간 그리드        | 미구현                   | 현재는 계산된 후보별 3상태 응답 화면을 사용함                                                                                                             |
| 사용자 검증·지표              | 미실시 기록              | 저장소에서 사용자 조사나 성과 지표를 확인하지 않음                                                                                                        |
| 도메인 테스트·CI              | 설정 및 테스트 코드 확인 | `src/domain/domain.test.ts`와 `test:domain` 스크립트가 있으며, `.github/workflows/domain-ci.yml`이 타입 검사·도메인 테스트·lint·build를 실행하도록 구성됨 |

따라서 이 문서에서 “검증”은 사용자 가치가 입증되었다는 뜻이 아니라, 현재 코드·테스트·설계 문서에서 확인 가능한 구현 상태를 뜻합니다. 테스트가 존재한다는 사실과 특정 실행 결과는 구분해야 합니다. 사용자 조사나 제품 성과 지표를 대신하지 않으며, 도메인 함수는 UI와 분리되어 있어 입력·출력 계약 중심으로 확장할 수 있습니다.

## What changed

제품 모델 관점에서 바뀐 핵심은 다음과 같습니다.

| 이전 중심                               | 현재 결정                                                      |
| --------------------------------------- | -------------------------------------------------------------- |
| 주최자가 정확한 후보 3~5개를 직접 선택  | 주최자가 조율 가능 시간대를 선택하고 후보는 제품이 계산        |
| 참석자가 후보마다 반복 응답             | v2.3 목표는 시간대 가용성 수집; 현재 구현은 후보별 응답을 유지 |
| 겹치는 후보를 입력 화면에서 막거나 설명 | 파생 후보끼리 겹침을 허용하고 주최자 결정 화면에서 비교        |
| 확정 실패 후 새 후보부터 다시 조율      | 기존 링크·응답을 보존하고 필요한 시간만 추가 확인              |

이 표는 릴리스 이력이 아니라 설계 기준과 현재 구현의 차이를 설명합니다. v2.3은 설계 방향으로 채택되었고, 현재 저장소는 그중 주최자 입력과 후보 파생을 먼저 반영한 상태입니다.

## Next validation

다음 검증은 구현 완료를 전제로 하지 않고, 아래 질문에 답하는 순서로 진행합니다.

1. 주최자가 `회의를 잡아도 되는 시간대`를 자신의 가용시간으로 이해하는지 확인합니다.
2. 09:00~12:00과 90분 확보 시간을 입력했을 때, 4개의 겹치는 시작 후보가 같은 범위에서 파생된다는 관계를 설명 없이 이해하는지 확인합니다.
3. 참석자 가용시간 그리드에서 `가능`, `일정 조정하면 가능`, `어려움`, `미응답`의 차이를 이해하는지 확인합니다.
4. 일부 시간대가 새로 열릴 때 기존 응답은 유지되고 새 칸만 다시 답하면 된다는 흐름을 확인합니다.
5. 파생 후보가 많아졌을 때 주최자가 상태·근거·다음 행동을 한 화면에서 비교할 수 있는 범위를 확인합니다.
6. 위 흐름을 검증한 뒤 셀 배열과 병합 구간 중 저장 표현을 확정하고, 참석자 가용시간을 `evaluateCandidates`에 연결합니다.

관련 결정 기록은 [조율 가능 시간대 모델 v2.3](docs/product/availability-window-model-decision-v2.3.md), [UX 아키텍처](docs/design/architecture.md), [제품 명세](docs/product/prd.md)에서 확인할 수 있습니다.
