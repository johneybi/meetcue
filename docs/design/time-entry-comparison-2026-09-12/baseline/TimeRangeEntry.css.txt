.time-range-entry {
  display: grid;
  gap: 24px;
}
.time-range-entry fieldset {
  position: relative;
  min-width: 0;
  margin: 0;
  padding: 0;
  border: 0;
}
.time-range-dates legend {
  margin-bottom: 12px;
  padding: 0;
  font-size: 14px;
  font-weight: 600;
}
.time-range-select-all {
  position: absolute;
  right: 0;
  top: -42px;
  min-height: 44px;
  padding: 0;
  border: 0;
  background: transparent;
  color: var(--mc-color-text-muted);
  font-size: 13px;
  text-decoration: underline;
  text-underline-offset: 3px;
}
.time-range-date-options {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: 6px;
}
.time-range-date-options label {
  position: relative;
  cursor: pointer;
}
.time-range-date-options input {
  position: absolute;
  opacity: 0;
  width: 100%;
  height: 100%;
  margin: 0;
  cursor: inherit;
}
.time-range-date-options label > span {
  min-height: 70px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  border: 1px solid var(--mc-soft-border);
  border-radius: var(--mc-radius-control);
  background: var(--mc-color-surface);
  color: var(--mc-color-text-muted);
}
.time-range-date-options small {
  font-size: 12px;
}
.time-range-date-options strong {
  font-size: 16px;
  font-variant-numeric: tabular-nums;
}
.time-range-date-options svg {
  visibility: hidden;
}
.time-range-date-options input:checked + span {
  background: var(--mc-soft-selected-fill);
  border-color: var(--mc-soft-selected-border);
  color: var(--mc-color-accent-pressed);
}
.time-range-date-options input:checked + span svg {
  visibility: visible;
}
.time-range-date-options input:disabled + span {
  border-color: transparent;
  color: var(--mc-color-disabled);
  cursor: not-allowed;
}
.time-range-entry button:focus-visible,
.time-range-date-options input:focus-visible + span {
  outline: 2px solid var(--mc-color-accent);
  outline-offset: 3px;
}
.time-range-fields {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 12px minmax(0, 1fr);
  align-items: end;
  gap: 8px;
}
.time-range-fields label {
  display: grid;
  gap: 8px;
  font-size: 14px;
  font-weight: 600;
}
.time-range-fields > span {
  padding-bottom: 12px;
  color: var(--mc-color-text-muted);
}
.time-range-fields select {
  width: 100%;
  min-width: 0;
  height: 48px;
  padding: 0 10px;
  appearance: auto;
  border: 1px solid var(--mc-color-border-strong);
  border-radius: var(--mc-radius-control);
  background: var(--mc-color-surface);
  color: var(--mc-color-text-primary);
  font-size: 16px;
  font-variant-numeric: tabular-nums;
}
.time-range-fields select:focus {
  outline: 2px solid var(--mc-color-accent);
  outline-offset: 2px;
}
.time-range-fields select[aria-invalid='true'] {
  border-color: var(--mc-color-danger);
}
.time-range-apply {
  display: grid;
  gap: 12px;
  padding-top: 4px;
}
.time-range-apply p {
  margin: 0;
  min-height: 20px;
  font-size: 13px;
  color: var(--mc-color-text-muted);
  line-height: 1.6;
}
.time-range-apply p.is-error {
  color: var(--mc-color-danger);
}
@media (max-width: 600px) {
  .time-range-entry {
    gap: 20px;
  }
  .time-range-date-options {
    gap: 4px;
  }
}
